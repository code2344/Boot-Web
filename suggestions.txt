SUGGESTION 1: Typo in CSS class causes broken hover effect
===========================================================

File: components/sections/faqs.tsx, line 31

WHAT THE CURRENT CODE DOES:
  <a href="https://hackclub.com" target="_blank" className="underline hover:decortaion-wavy">

The link is given the Tailwind class "hover:decortaion-wavy". Tailwind (or any
CSS utility framework) will simply ignore an unrecognised class name silently,
so no wavy-underline decoration ever appears when the user hovers over the link.

WHY IT IS WRONG:
  "decortaion" is a misspelling of "decoration". The intended class is
  "hover:decoration-wavy", which is a valid Tailwind CSS modifier. Because the
  class name does not exist, the hover animation is completely non-functional.
  Users never see any visual feedback when hovering over the Hack Club link.

PROPOSED SOLUTION:
  Change "hover:decortaion-wavy" → "hover:decoration-wavy":

    <a href="https://hackclub.com" target="_blank"
       className="underline hover:decoration-wavy">

  This restores the intended wavy-underline hover effect on the link.

STATUS: FIXED


SUGGESTION 2: w-screen on a child element causes horizontal overflow / scrollbar
=================================================================================

File: app/(website)/layout.tsx, line 14

WHAT THE CURRENT CODE DOES:
  <main className="max-w-4xl w-screen mx-auto p-4">{children}</main>

  The <main> element is given the Tailwind class "w-screen", which sets its
  CSS width to 100vw (100% of the viewport width).

WHY IT IS WRONG:
  100vw always refers to the full width of the browser viewport, including the
  area taken up by the vertical scrollbar. On desktop browsers that display a
  visible scrollbar, an element with width: 100vw inside a parent that is
  already at the scrollbar edge will be slightly wider than the available
  space, creating an unwanted horizontal scrollbar. The issue becomes more
  noticeable on pages with tall content (long scroll). The correct utility for
  "fill the available width of my parent" is "w-full" (width: 100%), not
  "w-screen" (width: 100vw).

  Note: max-w-4xl already caps the element at 56 rem, so w-screen only
  matters at viewport widths below 56 rem (small screens), which is exactly
  where scrollbar overlap causes visible layout breakage.

PROPOSED SOLUTION:
  Replace "w-screen" with "w-full":

    <main className="max-w-4xl w-full mx-auto p-4">{children}</main>

  "w-full" expands to fill the parent container's width (respecting the
  scrollbar), while "max-w-4xl" still caps it at 56 rem on wider screens.
  This eliminates the potential horizontal scrollbar without changing any
  visual design intent.

STATUS: FIXED


SUGGESTION 3: Missing question mark in FAQ heading
===================================================

File: components/sections/faqs.tsx, line 23

WHAT THE CURRENT CODE DOES:
  <h3 className="font-semibold mb-2">Can I make multiple OSes</h3>

  The heading text ends without a question mark even though it is phrased as
  a question.

WHY IT IS WRONG:
  All other FAQ headings in the same component end with a question mark
  ("Who can participate?", "Don't know how to make an OS?", "What is Hack
  Club?"). The missing punctuation is inconsistent and looks like an
  unfinished sentence to the reader.

PROPOSED SOLUTION:
  Add the missing question mark:

    <h3 className="font-semibold mb-2">Can I make multiple OSes?</h3>

STATUS: FIXED


SUGGESTION 4: Unused import causes dead code (Lightbulb icon in whatcounts.tsx)
=================================================================================

File: components/sections/whatcounts.tsx, line 2

WHAT THE CURRENT CODE DOES:
  import { Lightbulb } from "lucide-react";

  The Lightbulb icon is imported from the lucide-react library but is never
  referenced anywhere in the component's JSX or logic.

WHY IT IS WRONG:
  Dead imports increase the compiled bundle size slightly, clutter the module
  graph, and will trigger an ESLint "no-unused-vars" warning. They also
  mislead future developers into thinking the icon is used somewhere.

PROPOSED SOLUTION:
  Remove the unused import line entirely:

    - import { Lightbulb } from "lucide-react";

STATUS: FIXED


SUGGESTION 5: Heading hierarchy violation — <h2> inside an <h2> section
========================================================================

File: components/sections/whatcounts.tsx, lines 10 and 14

WHAT THE CURRENT CODE DOES:
  <h2 className="text-primary text-2xl">What Counts as an OS?</h2>
  ...
  <h2 className="text-xl">From Scratch</h2>
  ...
  <h2 className="text-xl">Base it off</h2>

  The section title uses <h2> and the two card titles inside also use <h2>.

WHY IT IS WRONG:
  HTML heading levels must form a strict hierarchy (h1 -> h2 -> h3 ...).
  Placing <h2> elements inside another <h2>'s content breaks the document
  outline, confuses screen readers, and harms SEO. The card headings are
  logically sub-headings of the section, so they belong at the next level
  down.

PROPOSED SOLUTION:
  Change the two inner headings from <h2> to <h3> (keeping all existing
  classes unchanged):

    <h3 className="text-xl">From Scratch</h3>
    <h3 className="text-xl">Base it off</h3>

STATUS: FIXED


SUGGESTION 6: Dead code — formatNumber function defined but never called
=========================================================================

File: components/countdown.tsx, lines 46-48

WHAT THE CURRENT CODE DOES:
  const formatNumber = (num: number): string => {
      return num.toString().padStart(2, "0");
  };

  The function is defined inside the Countdown component but is never called.
  The TimeUnit sub-component at the bottom of the file contains its own
  identical formatting logic inline (value.toString().padStart(2, "0")).

WHY IT IS WRONG:
  Dead code adds noise, increases cognitive load for future maintainers, and
  will produce an ESLint "no-unused-vars" / TypeScript error in strict mode.
  The duplicated formatting logic also violates the DRY principle.

PROPOSED SOLUTION:
  Remove the unused formatNumber function. If the formatting needs to be
  shared in the future, it can be extracted into a named utility and used by
  both Countdown and TimeUnit:

    - const formatNumber = (num: number): string => {
    -     return num.toString().padStart(2, "0");
    - };

STATUS: FIXED
